ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

    database:
        url: ${DB_URL:jdbc:postgresql://localhost:5432/first_project_db}
        user: ${DB_USER:postgres}
        password: ${DB_PASSWORD:postgres}



